class UserProfile {
  String apiKey;
  String name;
  String email;
  // Add more fields as needed

  UserProfile({
    required this.apiKey,
    required this.name,
    required this.email,
    // Initialize other fields
  });
}
